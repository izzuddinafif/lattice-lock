# LatticeLock Environment Configuration
# Copy this file to .env and update the values as needed

# Database Configuration
POSTGRES_DB=latticelock
POSTGRES_USER=latticelock_user
POSTGRES_PASSWORD=latticelock_db_secure_password_2024!

# Redis Configuration
REDIS_PASSWORD=latticelock_redis_secure_password_2024!

# Backend Configuration
SECRET_KEY=latticelock_super_secret_key_change_in_production_2024!
DEBUG=false

# Grafana Configuration
GRAFANA_USER=admin
GRAFANA_PASSWORD=latticelock_grafana_secure_password_2024!

# Environment
ENVIRONMENT=production

# SSL Configuration (if using HTTPS)
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# Application Configuration
APP_NAME=LatticeLock
APP_VERSION=1.0.0
APP_DESCRIPTION="Dynamic Physical Cryptography System"
