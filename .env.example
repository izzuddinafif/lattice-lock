# LatticeLock Environment Configuration

# Redis Configuration (for docker-compose.yml)
REDIS_PASSWORD=latticelock_redis_2024

# Build-time Configuration (passed to Docker build)
PDF_API_BASE_URL=http://pdf-backend:8000

# Backend Environment (for backend/.env)
# See backend/.env.example for full backend configuration
